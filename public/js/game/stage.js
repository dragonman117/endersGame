/**
 * Stage object
 * Generates stages for the graphics class
 **/

const PIXI = require('pixi.js');

let Stage = function () {
    let paused = false;
    let updateController = NaN;

    let base = PIXI.Stage;

};